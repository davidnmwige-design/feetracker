# ─── Database ────────────────────────────────────────────────────────────────
# PostgreSQL connection string (Neon, Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
# Direct (non-pooled) DB connection used by prisma migrate/generate. In production set DATABASE_URL to
# Neon's POOLED url (host with -pooler) and DIRECT_URL to the direct url. Locally they are the same.
DIRECT_URL="postgresql://user:password@host/dbname?sslmode=require"

# ─── Authentication ───────────────────────────────────────────────────────────
# Used by NextAuth to sign session tokens. Must be at least 32 characters.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="replace-with-a-random-secret-at-least-32-chars"
AUTH_SECRET="replace-with-a-random-secret"

# The public URL of your deployment (no trailing slash)
# Change this to your custom domain when you go live e.g. https://elimupay.co.ke
NEXTAUTH_URL="https://feetracker.co.ke"

# The public app URL used in client-side code (no trailing slash)
# Update this to your custom domain when you go live e.g. https://elimupay.co.ke
NEXT_PUBLIC_APP_URL="https://feetracker-seven.vercel.app"

# ─── Email (SMTP — Gmail or Resend) ──────────────────────────────────────────
# Provider-agnostic. Leave EMAIL_HOST unset for Gmail; set it for Resend/other.
#
# For RESEND (recommended for production):
#   EMAIL_HOST=smtp.resend.com
#   EMAIL_PORT=465
#   EMAIL_USER=resend
#   EMAIL_PASS=re_your_api_key
#   EMAIL_FROM=fees@elimupay.co.ke   (a verified-domain address)
#
# For GMAIL (default): leave EMAIL_HOST/PORT unset; EMAIL_USER is the Gmail address and
# EMAIL_PASS is a Gmail App Password (https://myaccount.google.com/apppasswords).
EMAIL_HOST=""
EMAIL_PORT="587"
EMAIL_USER="support@feetracker.co.ke"
EMAIL_PASS="xxxx-xxxx-xxxx-xxxx"
# The visible "From" address. Defaults to EMAIL_USER if unset (fine for Gmail; set it for Resend).
EMAIL_FROM=""

# ─── Admin ────────────────────────────────────────────────────────────────────
# Email address that gets admin access at /admin/dashboard
ADMIN_EMAIL="admin@feetracker.co.ke"

# Email address that receives admin platform notifications (signups, upgrades, backups)
ADMIN_NOTIFICATION_EMAIL="admin@feetracker.co.ke"

# Secret key required to create the first admin account at /api/admin/setup
ADMIN_SECRET_KEY="replace-with-a-long-random-secret"

# ─── Encryption ───────────────────────────────────────────────────────────────
# AES-256 field-level encryption key — must be exactly 32 characters.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex').slice(0,32))"
# REQUIRED in production. Without this, sensitive fields (parentEmail) are stored as plaintext.
# Add this to Vercel → Project Settings → Environment Variables.
DATA_ENCRYPTION_KEY="your_32_character_random_string_here"

# ─── Safaricom Daraja API (real-time MPESA notifications) ────────────────────
# Get these from https://developer.safaricom.co.ke → My Apps
# Leave blank to disable Daraja integration.
DARAJA_CONSUMER_KEY=get_from_developer.safaricom.co.ke
DARAJA_CONSUMER_SECRET=get_from_developer.safaricom.co.ke
# Set to "production" when going live; leave as "sandbox" for testing
DARAJA_ENV=sandbox
# Shared secret embedded in the registered C2B callback URLs so the confirmation/validation
# endpoints can authenticate Safaricom's webhooks. REQUIRED to register callbacks and to accept
# payments. Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
# NOTE: after setting/rotating this, each school must re-run callback registration.
DARAJA_CALLBACK_SECRET=replace-with-a-long-random-secret
# ─── M-Pesa STK Push (Lipa Na M-Pesa Online) ────────────────────────────────
# Prompts a parent's phone to pay. Needs the Lipa Na M-Pesa Online passkey + the shortcode it is
# tied to. In sandbox use shortcode 174379 and the sandbox passkey from the Daraja portal.
# NOTE (multi-tenant): the passkey is bound to one shortcode. A single platform shortcode collects
# all STK payments here; per-school direct collection would require each school's own passkey
# (a future per-school field) and is the fund-flow decision still pending with the client.
DARAJA_SHORTCODE=174379
DARAJA_PASSKEY=get_from_developer.safaricom.co.ke

# ─── Cron jobs (Vercel) ──────────────────────────────────────────────────────
# Protects /api/cron/* routes. REQUIRED — the routes fail closed (401) if this is unset.
# Vercel Cron sends it automatically as "Authorization: Bearer <CRON_SECRET>".
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
CRON_SECRET=replace-with-a-long-random-secret

# ─── Upstash Redis (optional caching) ────────────────────────────────────────
# Create a free Redis database at upstash.com → copy REST URL and token
# If not set, caching is disabled gracefully (no errors)
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here

# ─── SMS (Celcom Africa bulk SMS) ────────────────────────────────────────────
# Get these from your Celcom Africa account (celcomafrica.com). CELCOM_SHORTCODE is your registered
# sender ID. Leave blank to disable SMS — reminders then fall back to email + WhatsApp links.
CELCOM_API_KEY=get_from_celcomafrica.com
CELCOM_PARTNER_ID=get_from_celcomafrica.com
CELCOM_SHORTCODE=YOUR_SENDER_ID
# Optional override; defaults to https://isms.celcomafrica.com/api/services
CELCOM_API_URL=

# ─── Sentry (optional error tracking) ────────────────────────────────────────
# Create a Next.js project at sentry.io → copy DSN
SENTRY_DSN=https://your-key@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-key@sentry.io/project-id

# ─── Crisp Chat (optional support chat) ──────────────────────────────────────
# Create account at crisp.chat → Settings → Website → copy Website ID
NEXT_PUBLIC_CRISP_WEBSITE_ID=your-crisp-website-id

# ─── Plausible Analytics (optional) ──────────────────────────────────────────
# Create account at plausible.io → add your domain
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=elimupay.co.ke

# ─── Staging ──────────────────────────────────────────────────────────────────
# Set to "true" on staging Vercel environment to show the red staging banner
NEXT_PUBLIC_IS_STAGING=false
